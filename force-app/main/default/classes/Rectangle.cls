public class Rectangle {
    public void Area(){
        integer len = 10, bre = 20 ,area;
        area = len * bre;
        system.debug('area ='+area);
    }
    public void perimeter(){
        integer len = 10, bre = 20 ,perimeter;
        perimeter = 2* (len + bre);
        system.debug('perimeter ='+perimeter);
    }
}